<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css" />
    <title>UN Countries</title>
</head>
<body tabindex="-1" cz-shortcut-listen="true">
    <!-- Component NAV BAR -->
    <%- include('partials/navbar', {page: '/un/countries'}) %>

    <!-- Component GRID SYSTEM -->
    <div class="container mx-auto p-4">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center ">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Countries</h1>
                        
                    <p class="py-6">
                        Browse countries, i.e., United Nations member states. Click the 'region' buttons to filter by regions such as:
                        <a href="/un/countries?region=Africa" class="btn btn-primary btn-xs">Africa</a>
                        <a href="/un/countries?region=Americas" class="btn btn-primary btn-xs">Americas</a>
                    </p>
                </div>
            </div>
        </div> 
        
        <div class="overflow-x-auto overflow-y-hidden">
            <table class="table table-zebra">
                <tbody>
                    <% countries.forEach(country => { %>
                        <tr>
                            <td>
                                <div class="avatar">
                                    <div class="rounded h-24 min-w-40 shadow-x1">
                                        <img src="<%= country.flag %>" alt="<%= country.name %>">
                                    </div>
                                </div>
                            </td>
                            <td>
                                <%= country.name %> | <a href="/un/countries?region=<%= country.region.name %>" class="btn btn-primary mt-2 btn-xs"><%= country.region.name %></a>
                            </td>
                            <td>
                                <span class="font-bold">Population: </span><%= country.population %> - <span class="font-bold">Capital:</span> <%= country.capital %>
                            </td>
                            <td>
                                <a href="/un/countries/<%= country.a2code %>" class="btn btn-primary btn-xs">Details</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>         
    </div>
</body>
</html>